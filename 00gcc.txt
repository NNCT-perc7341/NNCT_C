# ディレクトリの移動
  cd　(ファイルパス)

#ディレクトリの中身を表示
  ls

#現在のディレクトリパスを表示
  pwd

#コンパイル
  gcc (ファイル名)
  ファイル名を指定する場合は....
  gcc -o (出力ファイル名) (ファイル名)

#リネーム
  mv (変更前のファイル名) (変更後のファイル名)

#ファイルの実行
  ./(ファイル名)  //gccでコンパイルしたものは.outで出力される。

ターミナル起動 : ⌘ + ⌥ + T
ターミナル終了 : ⌘ + ⇧ + X
